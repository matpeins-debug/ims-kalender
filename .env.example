# ───────────────────────────────────────────────────────────
# IMS Kalender · Environment Variables
# Copy this file to .env and fill real values. Never commit .env!
# ───────────────────────────────────────────────────────────

# ── Supabase ──
# Dashboard → Settings → API
SUPABASE_URL=https://elwlptapzwwqhjuxnnws.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...          # public, OK für Frontend
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...  # NIEMALS ins Frontend!

# ── App ──
APP_BASE_URL=https://kalender.ims-box.at
PORT=3000
NODE_ENV=development

# ── Resend (Phase 2: Briefings) ──
# https://resend.com/api-keys
RESEND_API_KEY=re_...
RESEND_FROM=office@ims-box.at

# ── ams.erp SQL (Phase 2: Liefertermin-Sync) ──
AMS_SQL_SERVER=amsdb01.ims.local
AMS_SQL_DATABASE=AMS01
AMS_SQL_USER=iflow_readonly
AMS_SQL_PASSWORD=***

# ── Cron Secret (Vercel Cron Auth) ──
# Random string, schützt Cron-Endpoints vor externem Aufruf
CRON_SECRET=generate_me_with_openssl_rand_base64_32
